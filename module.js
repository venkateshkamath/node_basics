// modules
// we use require to import
const sayHi = require("./func"); //require takes the path
const names = require("./names");
require("./mind-grenade"); // imported and so it is invoked cause no export

// sayHi("John");
// sayHi(names.ronny);
// sayHi(names.raj);
